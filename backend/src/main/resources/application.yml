spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fooddb
    username: postgres
    password: quan
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8086
application:
  app:
    jwtSecrect: ${JWT_SECRET:default_jwt_secret_key_123456789}